import 'package:flutter/material.dart';

/// Centralized border radius constants for the Temu Kopling app.
class AppRadius {
  AppRadius._();

  static const double xs = 4.0;
  static const double sm = 8.0;
  static const double md = 10.0;
  static const double lg = 12.0;
  static const double xl = 14.0;
  static const double xxl = 16.0;
  static const double xxxl = 18.0;
  static const double card = 20.0;
  static const double dialog = 20.0;
  static const double pill = 24.0;
  static const double authCard = 28.0;
  static const double full = 30.0;
  static const double circle = 100.0;

  // Pre-built BorderRadius objects for common usage
  static final BorderRadius radiusXs = BorderRadius.circular(xs);
  static final BorderRadius radiusSm = BorderRadius.circular(sm);
  static final BorderRadius radiusMd = BorderRadius.circular(md);
  static final BorderRadius radiusLg = BorderRadius.circular(lg);
  static final BorderRadius radiusXl = BorderRadius.circular(xl);
  static final BorderRadius radiusXxl = BorderRadius.circular(xxl);
  static final BorderRadius radiusXxxl = BorderRadius.circular(xxxl);
  static final BorderRadius radiusCard = BorderRadius.circular(card);
  static final BorderRadius radiusDialog = BorderRadius.circular(dialog);
  static final BorderRadius radiusPill = BorderRadius.circular(pill);
  static final BorderRadius radiusAuthCard = BorderRadius.circular(authCard);
  static final BorderRadius radiusFull = BorderRadius.circular(full);
  static final BorderRadius radiusCircle = BorderRadius.circular(circle);
}
